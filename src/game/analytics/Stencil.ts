import { Board } from "../Board";

export const stencil = (board: Board, other: Board) => {
    // TODO: Create a new Board, which contains the overlap between this board and the other board. Offsets are allowed,
    // but the returned Board's pieces must be in the same place as in the original Board.
    return new Board();
}
